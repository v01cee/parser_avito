# Настройка переменных окружения

## Создание Telegram бота

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например: "Мой парсер Авито")
4. Введите username для бота (должен заканчиваться на `bot`, например: `my_avito_parser_bot`)
5. Скопируйте токен, который даст вам BotFather

## Настройка .env файла

Создайте файл `.env` в корне проекта со следующим содержимым:

```
TELEGRAM_BOT_TOKEN=ваш_токен_здесь
```

**Пример:**
```
TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz
```

⚠️ **Важно:** Никогда не публикуйте файл `.env` в публичных репозиториях!

## Проверка

После создания `.env` файла запустите бота:

```bash
python telegram_bot.py
```

Если все настроено правильно, вы увидите сообщение:
```
🤖 Бот запущен! Ожидаю команды...
💡 Используй /start в Telegram чтобы начать
```


